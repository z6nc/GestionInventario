# Guía para Desarrolladores 🛠️

Bienvenido al README para desarrolladores del proyecto Sistema de Gestión de Inventario para el Restaurante Don Shoshi. A continuación, encontrarás algunas instrucciones útiles para trabajar en el proyecto:

## Creación de Archivoss

### Archivos para el Frontend

1. Si estás creando archivos para el frontend, asegúrate de crearlos dentro de la carpeta `src`. Esta carpeta contiene todos los archivos relacionados con la interfaz de usuario y la lógica del cliente.

   Por ejemplo, puedes crear archivos HTML, CSS y JavaScript dentro de la carpeta `src` para desarrollar la interfaz de usuario del sistema.

### Archivos para el Backend

1. Si estás creando archivos para el backend, debes seguir la estructura del patrón de diseño MVC (Modelo-Vista-Controlador). En este caso, crea tus archivos dentro de la carpeta `mvc`, que contiene las distintas partes del sistema backend.

2. Por ejemplo, si estás creando un CRUD (Crear, Leer, Actualizar, Eliminar) para productos, primero crea una carpeta llamada `ProductoMVC` dentro de la carpeta `mvc`. 

3. Luego, dentro de esta carpeta, puedes crear tus archivos MVC relacionados con la gestión de productos, como `ProductoModel.php`, `ProductoController.php` y `ProductoView.php`.


## Diseño para la Web 🎨

Puedes utilizar diferentes herramientas y tecnologías para diseñar la interfaz web del sistema. A continuación se detallan las opciones disponibles:

1. ### Tailwind CSS
    Si decides utilizar Tailwind CSS, puedes comenzar a diseñar tu interfaz directamente en los archivos HTML, utilizando las clases proporcionadas por Tailwind.  es necesario incluir un este comando   en tu terminal de proyecto 
      ` npx tailwindcss -i ./src/input.css -o ./src/output.css --watch `

2. ### Bootstrap
    Si prefieres utilizar Bootstrap, asegúrate de incluir los siguientes enlaces en tu archivo HTML para utilizar la biblioteca de Bootstrap:
    `<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">`
    `<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>`

3. ### css Nativo
    Si optas por utilizar CSS nativo, asegúrate de crear tu hoja de estilos dentro de la carpeta `src/css`. El nombre de tu archivo CSS debe ser coherente y reflejar lo que estás desarrollando en tu archivo.







